<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sql.UserMapper">
    <insert id="insertUser">
        insert into users(id, current)
        VALUES (#{id}, #{current})
    </insert>

    <select id="selectUser" resultType="String">
        select u.current
        from users u
        where u.id = #{id}
    </select>

    <update id="update">
        update users
        set current = #{current}
        where id = #{id}
    </update>

    <select id="selectUserOldWay" resultType="model.User">
        select u.id,
               u.last_message as "lastMessageId",
               u.last_dialog as "lastDialogId",
               u.ls_offset as "offset",
               u.last_search as "query",
               u.current as "state",
               u.fallback,
               u.search_offset as "searchOffset",
               u.lang
        from users u
        where u.id = #{id}
    </select>
</mapper>
